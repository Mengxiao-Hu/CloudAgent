---
paths:
  - "demo/frontend/**"
---

# Frontend (minimal demo UI)

Ownership: **frontend-developer** per `.claude/docs/ROLE_CONTRACTS.md`.

- Submit prompt, poll `GET /tasks/{id}`, render markdown report
- Use API shapes from `.claude/docs/SPECS.md` only
- No auth, no design system — minimal demo only
- **Do not** implement backend or agent loop
