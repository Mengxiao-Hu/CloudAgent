"""Demo module with intentional TODOs for agent analysis."""

def greet(name: str) -> str:
    # TODO: validate name length
    return f"Hello, {name}"


def load_config():
    # TODO: read from env instead of hardcoding
    return {"debug": True}
